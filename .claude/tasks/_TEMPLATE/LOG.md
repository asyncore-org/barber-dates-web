# LOG — TASK-<ID>

> Append-only. Cada entrada: timestamp + acción significativa. No logear cada edit (eso va en `files.md`).
> Tipos de entrada sugeridos: `start`, `checkpoint`, `decision`, `block`, `unblock`, `commit`, `discovery`, `resume`, `done`.

---

## YYYY-MM-DD HH:mm — start

Tarea creada. Rama `<tipo>/<slug>` creada desde `<base>`.
